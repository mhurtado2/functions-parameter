const quarterValue = (value) => {
    // make a variable that is the value quarted
    const quarter = value / 4


    // Invoke the function and store the return value
    //return the value of quarter
    return quarter
}

// make a variable that calls the function with an argument

const returnedNumber = quarterValue(100)

// Log the return
console.log(returnedNumber)